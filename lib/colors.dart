import 'package:flutter/material.dart';

// Кольорова схема додатку
class AppColors {
  // Основні кольори
  static const Color primary = Color(0xFF5C6BC0);
  static const Color primaryLight = Color(0xFF8E99F3);
  static const Color primaryDark = Color(0xFF26418F);
  static const Color accent = Color(0xFFFF7043);

  // Фон та поверхні
  static const Color background = Color(0xFFF5F6FA);
  static const Color surface = Colors.white;
  static const Color cardShadow = Color(0x1A5C6BC0);

  // Категорії
  static const Color work = Color(0xFFFF9800);
  static const Color personal = Color(0xFF42A5F5);
  static const Color study = Color(0xFF66BB6A);
  static const Color shopping = Color(0xFFEF5350);

  // Статус
  static const Color completed = Color(0xFF4CAF50);
  static const Color pending = Color(0xFFBDBDBD);

  // Пріоритет
  static const Color high = Color(0xFFEF5350);
  static const Color medium = Color(0xFFFF9800);
  static const Color low = Color(0xFF4CAF50);

  // Текст
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Допоміжні
  static const Color divider = Color(0xFFEEEEEE);
  static const Color deleteRed = Color(0xFFE53935);
}